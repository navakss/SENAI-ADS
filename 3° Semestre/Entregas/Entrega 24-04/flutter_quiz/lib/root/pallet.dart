import 'package:flutter/material.dart';

class Pallet {
  // Cores principais
  static const Color background = Color(0xFF1A1A2E);
  static const Color surface = Color(0xFF16213E);
  static const Color surfaceVariant = Color(0xFF0F3460);

  // Cores de destaque
  static const Color primary = Color(0xFF00D4FF);
  static const Color primaryDark = Color(0xFF0099BB);

  // Feedback
  static const Color success = Color(0xFF00C897);
  static const Color error = Color(0xFFFF4757);
  static const Color warning = Color(0xFFFFD700);

  // Texto
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFAAAAAA);
  static const Color textDisabled = Color(0xFF555555);

  // Bordas
  static const Color border = Color(0xFF00D4FF);
  static const Color borderSubtle = Color(0xFF1E2A4A);
}
